import { createMuiTheme } from '@material-ui/core/styles';


const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#4db6ac',
      contrastText: '#fff',
    },
    secondary: {
      main: '#b2dfdb',
    },
  },
});

export default theme;